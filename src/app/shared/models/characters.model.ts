export interface ICharacters {
    name: string;
    gender: string;
    origin: string;
    createdAt: string | Date;
    image: string;
}
export interface IChrDetail{
    img: string;
    currentStatus: number;
}