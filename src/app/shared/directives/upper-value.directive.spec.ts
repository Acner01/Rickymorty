import { UpperValueDirective } from './upper-value.directive';

describe('UpperValueDirective', () => {
  it('should create an instance', () => {
    const directive = new UpperValueDirective();
    expect(directive).toBeTruthy();
  });
});
